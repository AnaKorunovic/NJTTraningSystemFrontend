import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'njt-traningSystem',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
